{
  "name": "sw-mints-proxy",
  "version": "0.0.0",
  "scripts": {
    "build:sw": "vite build --config vite.config.sw.ts",
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview"
  },
  "devDependencies": {
    "@mizchi/mints": "workspace:*",
    "typescript": "^4.3.2",
    "vite": "^2.6.4"
  }
}
