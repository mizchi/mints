{
  "name": "@mizchi/mints-playground",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build-deps": "yarn workspace @mizchi/mints-tokenized build",
    "build": "yarn build-deps && vite build",
    "dev": "yarn build-deps && vite"
  },
  "devDependencies": {
    "comlink": "^4.3.1",
    "vite": "^2.6.10"
  },
  "dependencies": {
    "vite": "^2.6.14"
  }
}
