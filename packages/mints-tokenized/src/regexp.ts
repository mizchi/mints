// export const re = /(?![ * / ])(?:\[(?:(?![ \] \\ ]).|\\.)*\]|(?![ / \] \\ ]).|\\.)*(/[$_\u200C\u200D\p{ID_Continue} ]*|\\)?/g
// export const re = /(?![ * / ])(?:\[(?:(?![\]\\]).|\\.)*\]|(?![ / \] \\ ]).|\\.)*(/[$_\u200C\u200D\p{ID_Continue} ]*|\\)?/g

const x = `/(?![ * / ])
(?:
  \[
  (?:
    (?![ \] \\ ]).
    |
    \\.
  )*
  \]
  |
  (?![ / \] \\ ]).
  |
  \\.
)*
(
  /
  [ $ _ \u200C \u200D \p{ID_Continue} ]*
  |
  \\
)?`;
